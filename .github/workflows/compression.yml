name: Compression

on:
  push:
    branches:
      - 'main'

jobs:
  Update-Compression-Files:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo content
        uses: actions/checkout@v3
      - name: setup python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'
      - run: pip install -r requirements.txt
      - run: pip install financedatabase
      - run : pip install openpyxl
      - name: Update Compressions
        uses: jannekem/run-python-script-action@v1
        with:
          script: |
            import financedatabase as fd
            import pandas as pd

            cryptos = pd.read_csv('Database/cryptos.csv')
            cryptos.to_csv('compression/cryptos.gzip', index=False, compression='gzip')

            currencies = pd.read_csv('Database/currencies.csv')
            currencies.to_csv('compression/currencies.gzip', index=False, compression='gzip')

            equities = pd.read_csv('Database/equities.csv')
            equities.to_csv('compression/equities.gzip', index=False, compression='gzip')

            etfs = pd.read_csv('Database/etfs.csv')
            etfs.to_csv('compression/etfs.gzip', index=False, compression='gzip')

            funds = pd.read_csv('Database/funds.csv')
            funds.to_csv('compression/funds.gzip', index=False, compression='gzip')

            indices = pd.read_csv('Database/indices.csv')
            indices.to_csv('compression/indices.gzip', index=False, compression='gzip')

            moneymarkets = pd.read_csv('Database/moneymarkets.csv')
            moneymarkets.to_csv('compression/moneymarkets.gzip', index=False, compression='gzip')
      - name: Commit files and log
        run: |
          git config --global user.name 'GitHub Action'
          git config --global user.email 'action@github.com'
          git add -A
          git checkout main
          git diff-index --quiet HEAD || git commit -am "Update Compression Files"
          git push
      - name: Check run status
        if: steps.run.outputs.status != '0'
        run: exit "${{ steps.run.outputs.status }}"



